# 🚀 Simple Web App

This is a demo project for deploying a web application on an OVH VPS using **Jenkins + Docker**.

## Features
- Static HTML + CSS page
- Dockerized with Nginx
- Ready for CI/CD pipeline

## Run Locally
```bash
docker compose up --build -d
